/* eslint-disable no-undef */
const { expect } = require('chai');

const helpers = require('../utils/helper');

describe('saveSVG()', () => {
  it('save file content into a file', () => {
    expect(5 + 5).to.be.equal(10);
  });
});
